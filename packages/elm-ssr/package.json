{
  "name": "@elm-ssr/elm-ssr",
  "version": "0.1.0",
  "description": "Authoring API for elm-ssr: the Elm modules (Route, Loader, Action, Html, Svg, Island, …) consumed by elm-ssr apps. The @elm-ssr/cli build syncs these into each app's .elm-ssr/src/ at compile time.",
  "license": "MIT",
  "private": false,
  "files": [
    "src",
    "elm.json"
  ],
  "engines": {
    "bun": ">=1.3"
  }
}
